server:
  port: 8081

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/GnyanRanjanBehera/Task-Pay-Config.git
          clone-on-start: true

